#include "monty.h"
/**
 * add - Add top two items of stack
 * @stack: the stack
 * @line: line number
 */
void add(stack_t **stack, unsigned int line)
{
    // if there aren't two items in stack, failure

    // stack nodetwo.n += stack nodeone.n
    // pop(stack, line);
}
