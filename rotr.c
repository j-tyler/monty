#include "monty.h"
/**
 * _rotr - Bottom element of the stack rotates to the top
 * @stack: the stack
 * @line: line number
 */
void _rotr(stack_t **stack, unsigned int line)
{
	 (void) stack;
	 (void) line;
}
